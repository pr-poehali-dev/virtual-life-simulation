
// Простой компонент-заглушка для анимаций
import { ReactNode } from "react";

export const Motion = ({ children }: { children: ReactNode }) => {
  return <>{children}</>;
};

export const AnimatePresence = ({ children }: { children: ReactNode }) => {
  return <>{children}</>;
};
